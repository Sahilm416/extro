---
"create-extro": minor
---

Add `create-extro`, the scaffolder for new Extro extensions. Run `pnpm create extro` (or the npm/yarn/bun equivalent) to generate a project from the `default` or `minimal` template, with optional dependency install and git init.
